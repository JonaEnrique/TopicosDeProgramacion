#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i, lim = 0, tamano = 0, cantlineas = 0, limLineas = 2;
    int condicion1, condicion2, condicionFinal, lineas;
    int valor;
    short char cad[7]={};

    // crearPalabra
    FILE *pf = fopen("Ejemplo.txt", "wt");
    if(fp)
    {

        cad[7]
        scanf("%sd", &cad);
        fprintf(fp, "%d", cad);
        fclose(fp);
        return 0
    }

    while(condicionFinal == 1 && totalLineas <= limLineas)
    {
        topeCantNumero = rand()%10;
        printf("Ingrese numero para grabar");
        scanf("%sd", numero, i)
        if((tamano = tamlinea(cad)) = 1)
            condicion1 = 1;
        if((tamano = tamlinea(cad)) = 7)
            condicion2 = 1;
        if(condicion1 & condicion2)
        {
            limLineas = srand()%10;
            condicionFinal = 1;
        }
        totalLineas = contadorLineas(cad, topeCantNumero)


    }

    insertarNumero(cad);

    return 0;
}

insertarNumero(*int cad, int topeCantNumero)
{

}
